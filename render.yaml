services:
  - type: web
    name: llm-quiz-solver
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install --with-deps chromium
    startCommand: gunicorn -w 2 -b 0.0.0.0:$PORT app:app --timeout 300
